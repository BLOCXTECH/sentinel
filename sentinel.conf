# specify path to blocx.conf or leave blank
# default is the same as BlocX
#blocx_conf=/home/root/.blocx/blocx.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
# network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
